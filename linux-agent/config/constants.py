"""Agent constants.

Will hold fixed, non-configurable values used across the Linux Agent
(e.g. default collection intervals, file paths, or protocol identifiers),
once implemented. No constant values are defined here yet.
"""
