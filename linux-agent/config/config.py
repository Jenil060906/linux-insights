"""Agent configuration loading.

Will be responsible for loading and validating the Linux Agent's runtime
configuration (e.g. backend endpoint, agent identity, collection interval,
authentication credentials), once implemented. Configuration values
themselves are not defined here yet.
"""
